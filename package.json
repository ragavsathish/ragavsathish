{
  "scripts": {
    "generate:rdf-graph": "node scripts/generate-rdf-graph.mjs",
    "dry": "npm run generate:rdf-graph && git diff --check"
  },
  "dependencies": {
    "@hpcc-js/wasm": "^2.35.0",
    "n3": "^2.1.1",
    "ts-graphviz": "^3.0.7"
  }
}
